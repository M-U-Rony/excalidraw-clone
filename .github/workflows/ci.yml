name: Learning CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.19.0'

    - name: Install pnpm
      run: npm install -g pnpm

    - name: generate prisma client
      run: cd packages/db && pnpm exec prisma generate && cd ../..

    - name: Install dependencies
      run: pnpm install

    - name: Run Build
      run: pnpm run build